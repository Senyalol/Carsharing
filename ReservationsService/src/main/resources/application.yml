spring:
    application:
        name: ReservationsService
    datasource:
        url: jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
        username: ${POSTGRES_USERNAME}
        password: ${POSTGRES_PASSWORD}
        driver-class-name: org.postgresql.Driver
    kafka:
        consumer:
            key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
            value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
            properties:
                spring:
                    json:
                        value:
                            default:
                                type: com.shared.dto.ShortCarInfoDTO
                        trusted:
                            packages: '*'

    flyway:
        repair-on-migrate: true
        validate-on-migrate: false
        enabled: true
        locations: classpath:db/migration
#        clean-on-validation-error: true
        schemas: public
        default-schema: public
        #        baseline-on-migrate: true

    jpa:
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect

server:
    port: 8082

#spring.application.name=ReservationsService
#
#server.port=8082
#
#spring.datasource.url=jdbc:postgresql://localhost:5432/Carsharing
#spring.datasource.username=postgres
#spring.datasource.password=1111
#spring.datasource.driver-class-name=org.postgresql.Driver
#
#spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
#spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer
#
#spring.kafka.consumer.properties.spring.json.value.default.type=com.shared.dto.ShortCarInfoDTO
#spring.kafka.consumer.properties.spring.json.trusted.packages=*